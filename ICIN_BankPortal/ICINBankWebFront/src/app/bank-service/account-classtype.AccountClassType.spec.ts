import { AccountClasstype } from './account-classtype.AccountClassType';

describe('AccountClasstype', () => {
  it('should create an instance', () => {
    expect(new AccountClasstype()).toBeTruthy();
  });
});
